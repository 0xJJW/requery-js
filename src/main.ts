import { defineComponent, queryComponent } from "@/component";
import { computed, effect, reactive, ref, toRaw, watch } from "@vue/reactivity";

export {
  computed,
  defineComponent,
  effect,
  queryComponent,
  reactive,
  ref,
  toRaw,
  watch,
};
